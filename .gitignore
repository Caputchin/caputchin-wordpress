/vendor/
/node_modules/
*.log
.phpunit.result.cache
.phpcs.cache

# Local Docker test environment state + the local site key/secret compose loads
/e2e/.wp-data/
/e2e/.env

# Maintainer-local compose override (points the test env at a local dev stack)
/e2e/docker-compose.override.yml
